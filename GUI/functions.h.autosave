#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <QListWidget>

#endif // FUNCTIONS_H

QString listTraversal(QList items)
{
    for(int j=0;j<items.count();j++)
    {
        QString list= items.at(j)->text();
        ui->listWidget->insertItem(j,list);
    }
    return list;
}
